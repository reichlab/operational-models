This is a no-op model for testing Docker image build and deploy. Generates no output .csv or .pdf files and so will cause the container to fail when it runs.

Notes:

- **requirements.in**: just click
- **renv.lock**: minimal to work with "renv::restore()"
